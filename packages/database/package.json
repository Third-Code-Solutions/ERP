{
  "name": "@third-code-erp/database",
  "version": "0.1.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "studio": "drizzle-kit studio",
    "db:seed": "tsx src/seed.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@third-code-erp/shared-types": "workspace:*",
    "drizzle-orm": "^0.40.1",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@third-code-erp/config": "workspace:*",
    "@types/node": "^22.0.0",
    "drizzle-kit": "^0.30.5",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4"
  }
}
