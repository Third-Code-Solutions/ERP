# Agent 15 public book-demo brand repair

## Scope and outcome

The public `/book-demo` surface now uses the established ABI OPS identity:

- the header identifies the organization as `Actuate Builders`; and
- the required contact-consent sentence names `Actuate Builders Inc.`.

Only public-facing wording changed. The form fields, required consent control,
accessibility attributes, validation, server action, success/error states,
routing, and demo-request persistence are unchanged. No owner-route,
provider, production, verifier-policy, or generated file was modified.

## Changed files

- `apps/web/src/app/book-demo/demo-request-form.tsx`
- `apps/web/src/app/book-demo/page.tsx`

## Node 22 verification

All commands used Node `v22.23.2` and pnpm `10.33.0`.

| Command | Result |
| --- | --- |
| `pnpm exec eslint --max-warnings=0 apps/web/src/app/book-demo/demo-request-form.tsx apps/web/src/app/book-demo/page.tsx` | **PASS**. |
| Focused `findLegacyBrandViolations` assertion over the two changed source files | **PASS** — 2/2 files have zero legacy-brand violations. |
| `pnpm --filter @third-code-erp/web run typecheck` | **PASS**. |
| `pnpm --filter @third-code-erp/web run build` | **PASS** — optimized Next.js build compiled, type-checked, and generated 87 pages; `/book-demo` is present in the route manifest. |
| `pnpm test:abi-ops-brand` | **EXPECTED STAGE FAILURE** — verifier-policy test passes, while the repository-clean test reports only the separately assigned `apps/web/src/app/owner/page.tsx` match and its regenerated `.next` output. |
| `pnpm verify:abi-ops-brand` | **EXPECTED STAGE FAILURE** — only the same Agent 03 owner-route source and regenerated build match remain. The regenerated build has no `Third Code Solutions` match. |

The ignored `apps/web/.next` directory was regenerated by the successful build
and remains untracked. It is not part of this changeset.

## Handoff

→ Handoff to Agent 03. Reason: both public book-demo source matches are clean;
the remaining verified legacy string is on the authenticated owner route.
Inputs: this isolated source diff, the passing lint/type/build evidence, and the
expected owner-only brand-verifier output. Expected output: replace the single
owner-page brand string while preserving owner-only access, non-indexing,
platform data authority, and every existing control. Production remains
**NO-GO**.
