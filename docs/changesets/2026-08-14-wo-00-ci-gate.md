# BUILD OPS WO-00 CI gate

## Outcome

Restored the WO-00 invariant gate that was present in the repository's prior
Build Ops implementation but absent from the current branch's root scripts and
pull-request workflow.

- Added root commands for the invariant scanner and its executable tests.
- Added a required `BUILD OPS Invariants` GitHub Actions job on pushes to main
  and pull requests targeting main.
- The job runs the three negative-path scanner tests and scans changed
  `supabase/migrations/*.sql` files for monetary precision and tenant-id
  violations.
- The Postgres reproducibility job now runs the database E2E-prefix invariant
  after the disposable reset, allowing only the seeded `abi-ops-local` tenant.
- The build is blocked on the invariant job and the database invariant job.

## Files changed

- `.github/workflows/ci.yml`
- `package.json`
- `scripts/verify-build-ops-invariants.test.mjs`
- `docs/changesets/2026-08-14-wo-00-ci-gate.md`

## Safety

No schema, migration, application data, deployment, or production workflow was
executed. The existing invariant implementation and its tests were reused.

## Validation

- PASS — `pnpm test:build-ops-invariants`: 7/7, including CI wiring.
- PASS — `pnpm verify:build-ops-invariants`: current migration directory.
- PASS — `node scripts/verify-workflow-action-refs.mjs`.
- PASS — `node scripts/run-actionlint.mjs` (actionlint 1.7.12).
- PASS — `git diff --check`.
- BLOCKED — local database invariant execution; Docker Desktop is installed but
  its daemon is unavailable, and the Supabase CLI is not installed.
- NOT RUN — full workspace typecheck, build, unit suite, and E2E suite because
  the existing dependency installation/runtime blocker remains outside WO-00.

## Rollback

Revert the four implementation/documentation files above together. No database
rollback is required.
