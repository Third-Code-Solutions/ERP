# M3.48 - Landing GEO structured data

Expanded the landing page's linked structured-data graph with `WebSite`,
`WebPage`, and richer `SoftwareApplication` evidence while preserving the
existing Third Code ERP visual design. Added a pure builder and focused tests;
metadata now declares the en-PH language variant and website Open Graph
identity.

Changed source: `apps/web/src/app/page.tsx`,
`apps/web/src/lib/landing-structured-data.ts`, and its test.

Validation: focused landing/structured-data tests 5/5; Web 67 files/451 tests;
workspace lint/typecheck, `git diff --check`, and the 79/79-route production
build pass. Local production HTML returned 200 and contained the expected
brand/JSON-LD markers with no legacy ABI Ops, ERPNext, or Frappe identifiers.

Release boundary: no hosted SQL/data/Storage, Railway setting/deployment, or
Vercel build/promotion changed. Supabase remains non-ready because the ordered
branch replay is stopped by the duplicate tenant `PO-0002` `P0001` preflight;
Vercel remains disconnected and spend-protected.
