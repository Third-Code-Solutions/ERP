# CI gate hardening

Date: 2026-08-15

## Summary

- Recorded and verified the existing fixes for the web database-authority
  allowlist and the authenticated `public.users` read grant that caused the
  displayed Unit Tests and PostgreSQL 17 reproducibility failures.
- Added the same bounded 30-second harness timeout already used by the API
  procurement contract to the Inventory item-configuration contract. This
  prevents slow Nest application initialization on Windows/Node 24 from
  becoming a false 5-second test failure.
- Restored the three verifier scripts referenced by the production promotion
  workflow in the root package-script contract so the guarded workflow can
  execute its preflight checks.

## Verification

- PASS: `pnpm test:web-db-boundary` (4 tests).
- PASS: `pnpm test:build-ops-invariants` (7 tests).
- PASS: `pnpm verify:abi-ops-brand`, `pnpm verify:type-safety`, and
  `pnpm verify:app-router-boundaries` (the exact commands used by the
  production promotion workflow).
- PASS: `pnpm ci:actionlint`.
- PASS: `pnpm verify:workflow-action-refs`.
- PASS: `pnpm turbo test` (API 180 files/790 tests; web 898 tests; the
  database package reported 158 DB-integration skips because no local replay
  database was available).
- PASS: all CI unit, WO contract, database-release-plan, project-cutover-plan,
  controlled-release-plan, release-identity-plan, web-boundary, and Turbo
  cache scripts listed in `.github/workflows/ci.yml`.
- PASS: `pnpm turbo typecheck` and `pnpm turbo lint`.
- PASS: `pnpm build` (Next.js 85/85 static pages; Nest API webpack build).
- PASS: current remote workflow-dispatch run `31821375029` on commit
  `73f44027` had successful Unit Tests and Database Reproducibility jobs.
- BLOCKED locally: the Postgres 17 replay cannot be reproduced because Docker
  Desktop's Linux engine is unavailable, and the Supabase CLI/psql are not
  installed. The configured endpoint is not a replayed Supabase database
  (`public.tenants` is missing).
- BLOCKED before production promotion: the GitHub `Production` environment
  currently exposes no configured deployment secrets, and the workstation has
  no provider/database credentials.
- PASS: commit `fcb81b38` was pushed to the release branch and PR #2 was
  opened. Remote CI run `31874843833` passed all executed checks; E2E remained
  skipped because no E2E base URL is configured.

## Rollback

Revert the timeout-only change in
`apps/api/src/inventory/inventory-item-configuration.controller.spec.ts` and
remove this changeset. The database-authority and user-read fixes are already
part of the current branch history and should not be reverted as part of this
test-harness rollback.
