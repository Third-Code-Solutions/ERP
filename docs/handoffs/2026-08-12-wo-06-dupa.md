# WO-06 DUPA handoff — 2026-08-12

## State

→ Handoff to Agent 01 / Product and Agent 05 / API. Reason: the local M-03/M-04
database foundation is verified, but the canonical worked example is blocked by
an authority-document arithmetic contradiction and the user-facing/API layers
must wait for the owner decision.

## Inputs

- PRD WO-06 M-03/M-04 requirements and canonical worked example.
- `packages/shared-types/src/bom/dupa.ts` exact rational engine.
- `packages/database/src/schema/dupa-libraries.ts` and `dupas.ts`.
- `supabase/migrations/20260812180000_wo_06_dupa_engine.sql`.
- `docs/blockers/2026-08-12-wo-06-canonical-math-contradiction.md`.

## Verified outputs

- Local PostgreSQL 17/Redis 7.4.9 lane: 60 migrations applied from a clean
  database, 236/236 database tests without skips, and 3 API integration tests.
- WO-06 static, read-only database, and transactional behavior gates pass.
- Tenant-composite FKs, forced RLS, explicit authenticated/service grants,
  append-only audit triggers, classified-work guard, H sync, crew-role refresh,
  and trigger-owned computed totals are present.
- Hosted Supabase was not changed.

## Required next steps

1. Agent 01 obtains ABI's decision: provide source-precision rates, or revise
   canonical expected values to `G=1,621,751` and `H=16,217,506`.
2. Agent 05 adds validated DUPA/library API contracts after the math decision;
   no direct browser writes to computed totals.
3. Agent 02/10 adds UI only after the engine/canonical gate is unblocked.
4. Reconcile the provider migration ledger and duplicate `PO-0002` before any
   hosted migration or production deployment.
