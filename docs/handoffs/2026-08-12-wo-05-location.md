# WO-05 location dimension handoff

## Inputs

- BUILD OPS PRD v1.3, M-02 / WO-05
- WO-04 grain fields and review model
- Existing tenant-scoped BOM and project data model

## Completed

- Agent 04 scope: additive schema, migration, composite constraints, RLS,
  audit triggers, and database verifier.
- Agent 05 scope: typed parser boundary, server actions, approval gate, and
  tenant/project validation.
- BOM UI scope: review queue, location selector, project rollup, and responsive
  states.

## Required next handoff

-> Handoff to WO-06 / DUPA pricing implementation. Inputs: `kind`,
`unit_rate_source`, `classification_status`, `location_id`, and the pending
location-review gate. Expected output: exact-centavo DUPA pricing over the
resolved BOM model without reintroducing editable derived rates.

## Release gate

Do not apply the WO-04/WO-05 migrations to hosted Supabase until the provider
source ledger and duplicate PO-0002 data decision are reconciled. The hosted
project currently lacks these tables and columns.
