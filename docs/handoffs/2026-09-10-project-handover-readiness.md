# Handoff — project handover readiness

The turnover surface now exposes a read-only, source-derived readiness gate.
Existing document attachment and COC actions remain authoritative. Future
zone-level handover or financial close-out work must introduce its own
schema/API contract and preserve the explicit deferred blockers in the
projection notes.
