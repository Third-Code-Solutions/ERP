# Claim document workflow

User outcome: attach an existing project document by a readable, paginated selection instead of copying a UUID. Preserve claim certification, existing attachments, the commercial spine, role permissions and append-only audit history.

1. Agent 05 (Astra): define a strict Core attachment command with `clientRequestId`, `documentId`, existing `kind` values and optional caption. Recheck current membership and `document.manage`; lock claim before document; validate tenant/project and current status. Preserve the existing terminal statuses (`paid`, `rejected`, `cancelled`). Insert attachment and semantic audit in one transaction. Use attachment UUID identity for idempotent replay; identical committed replay remains valid after a terminal transition, but changed payload conflicts. Prove rejection, replay, audit rollback and concurrent behavior with focused tests. No Web or migration edits in this handoff.
2. Agent 03 (Luna, after contract handoff): route the existing action through Core, remove its direct writer, scope display joins, and implement honest paginated project-document selection with loading, empty, error, retry and draft-retention states. Do not reuse the certification selector's capped list or change certification behavior. Add focused component/action and browser verification.
3. Agent 04/12: separately review direct table privileges and any required forward migration, rollback and PostgreSQL privilege proofs. Production database changes require the existing recovery/rehearsal release evidence. Do not bypass the code-only production workflow or claim application checks replace database controls.
4. Independent review: verify authorization, tenant/project boundaries, lock order, retry identity, retained historical links and accessible UI behavior. Keep edits sequential on shared files.
5. Agent 13: run proportionate local, PostgreSQL, CI and browser gates; release only the verified complete slice through the protected workflow. No provider mutation, migration execution or new dependency is authorized by this handoff alone.

User acceptance clarification: professional UI/UX is required across the application, not only the dashboard. For this slice, verify the existing design tokens and typography, readable document names rather than raw IDs, responsive layouts at 320/768/1024/1440 px, keyboard and focus behavior, labelled controls, and explicit loading, empty, error and success states. Exercise pagination and stale-response handling in a real browser. An uncertain submission must retain its original request identity and immutable payload so retry cannot silently duplicate evidence. Route-render coverage alone is not proof of visual or interaction quality; record which screens and states were actually inspected, and do not claim whole-application polish from this slice.

PR #65 schedule promotion runs independently at commit `e45c1efc0464b2ce92fd01df7ed2b433b7cb9c7d`. This branch starts from that exact reviewed tree; claim work is not part of that release. KYC document selection remains a separate account-relationship contract.

Verification handoff to Agent 13: the installed WSL1 PostgreSQL 17 test runtime was recovered without resetting its existing databases. Newly created isolated databases exposed two stale test-only Supabase bootstrap fields: `storage.buckets.allowed_mime_types` and `auth.users.email_confirmed_at`, preventing existing September migrations from replaying. Hosted catalog inspection confirmed nullable `text[]` and `timestamptz`, respectively; add those fields to the test bootstrap and verify a fresh replay. This does not alter the managed production schema or its migration ledger.

Agent 05 handoff, 2026-09-12: claim command and document-delete retention guard are implemented. Independent main-agent execution passed all 14 PostgreSQL claim tests against the isolated 169-migration database. Reverse attach/delete contention uses a nonblocking tenant audit-chain admission before attachment insertion, returning a retryable conflict with no partial write. Existing global audit locking is unchanged. Agent 04/12 may now prepare the narrow forward privilege/index hardening and local proof, including review of every located writer, rollback implications and direct authenticated-role denial. This is local implementation authority only; hosted execution remains blocked on recovery evidence. Agent 13 owns final integration and deployment.

Additional Agent 03 handoff: main owns only the legacy project-document deletion action and its existing test file for a matching retention guard. The Core feature flag must not leave an alternate privileged deletion path capable of cascading retained claim evidence. Check the tenant/document attachment link after locking the document, before derived-row or document deletion; preserve unreferenced deletion and post-commit Storage cleanup. No overlap with the claim-form agent's files. Independent security review follows before release.

Additional Agent 13 handoff: `ci.yml` now includes a dedicated credential-free Chromium job for `e2e/claim-document-attach.spec.ts`. It uses pinned actions, Node 22, frozen pnpm dependencies, one worker, zero retries, bounded Playwright/job timeouts, the existing no-skips assertion and always-uploaded JSON, failure and responsive screenshot artifacts. The gate exercises only the temporary Vite harness and does not replace authenticated production E2E. Initial local Node 24 verification passed all six browser tests; the focus-fix follow-up below expands the local harness to nine tests. Node 22 CI execution remains pending.

Additional Agent 03 handoff: claim-document pagination now records keyboard pagination focus intent and restores focus only after the matching page response settles. If the requested control is disabled at a page boundary, focus moves to the enabled opposite pagination control. Focus intent is cleared on claim-scope/disabled cleanup, and a user move to another field is preserved without focus stealing. The browser harness covers nine interactions, including page 1 → 2 → 1 → 2 → 3 requests, endpoint fallback, user focus movement and scope changes; local Node 24 Chromium verification passed 9/9. No Core, migration or API changes are part of this focus fix.
